## Integrity

SHA-256 checksums are provided in `SHA256SUMS.txt`.

Verification (Linux/macOS):
`sha256sum -c SHA256SUMS.txt`

Verification (Windows PowerShell):
Use `Get-FileHash <file> -Algorithm SHA256` and compare with the published values.
